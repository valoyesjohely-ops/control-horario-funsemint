{
  "name": "control-horario-funsemint-functions",
  "description": "Firebase Functions for Auth↔Firestore synchronization",
  "version": "0.1.0",
  "engines": {
    "node": "18"
  },
  "main": "lib/index.js",
  "scripts": {
    "build": "tsc",
    "serve": "firebase emulators:start --only functions",
    "deploy": "firebase deploy --only functions"
  },
  "dependencies": {
    "firebase-admin": "^11.0.0",
    "firebase-functions": "^4.0.0"
  },
  "devDependencies": {
    "typescript": "^5.0.0",
    "tslib": "^2.0.0",
    "@types/node": "^18.0.0"
  }
}
